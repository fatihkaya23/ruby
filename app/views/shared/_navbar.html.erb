
      <!-- Static navbar -->
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
             <%=link_to t('Fatih Kaya'), posts_path, class:"navbar-brand"%>
          </div>
          <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
              <li class="active"><%= link_to(t('newpost'), new_post_path )%></li>
              <li><a href="/admin"><%=t('about')%></a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
              <% if user_signed_in? %>
                  <ul class="nav navbar-nav navbar-right">
                    <li><a href="#"><%=t('welcome')%> <%=current_user.name%></a></li>
                    <li><%= link_to t('exit'), destroy_user_session_path, :method => :delete %></li>
                    <li><%= link_to t('settings'), edit_user_registration_path%></li>
                  </ul>
               <% else %>
                  <ul class="nav navbar-nav navbar-right">
                    <li><%= link_to(t('login'), new_user_session_path, class:'glyphicon glyphicon-user') %></li>
                    <li><%= link_to(t('signup'), new_user_registration_path, class:'glyphicon glyphicon-log-in') %></li>
                  </ul>
              <% end %> 
            </ul>
          </div><!--/.nav-collapse -->
        </div><!--/.container-fluid -->
      </nav>  